<Window x:Class="TermoMatic_MVVM.ViewModels.VentanaEmergente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TermoMatic_MVVM.ViewModels"
        xmlns:vm="clr-namespace:TermoMatic_MVVM.ViewModels"
        mc:Ignorable="d"
        Title="Ventana Emergente" Height="200" Width="350">

    <Window.DataContext>
        <vm:VentanaEmergenteViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="10*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Viewbox Grid.Row="0">
                <Label Content="{Binding TextoMostrado}"/>
            </Viewbox>

            <Viewbox Grid.Row="1">
                <Button Content="{Binding TextoBoton}" Click="EventoBotonOk_Click" Padding="20,0"/>
            </Viewbox>
        </Grid>
    </Grid>

</Window>
